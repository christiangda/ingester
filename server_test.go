package server_test

import (
	"testing"
)

func TestNewServer(t *testing.T) {

	// var tests = []struct {
	// 	ip       string
	// 	port     int
	// 	protocol string
	// 	want     *server.Server
	// }{
	// 	{"127.0.0.1", 8080, "tcp",
	// 		&server.Server{},
	// 	},
	// }
	// for _, test := range tests {
	// 	if got := server.NewServer(test.ip, test.port, test.protocol); !reflect.DeepEqual(got, test.want) {
	// 		t.Errorf("NewServer(%v,%v,%v) = %v, and want is = %v", test.ip, test.port, test.protocol, got, test.want)
	// 	}
	// }
}
